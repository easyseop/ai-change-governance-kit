name: governance

on:
  pull_request:

jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - name: change intent
        continue-on-error: True
        run: python3 .harness/gates/check-change-intent.py "$@"
