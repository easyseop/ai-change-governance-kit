policy_version: "test-invalid-maturity"

defaults:
  unlisted_level: free
  block_levels: [frozen]
  approve_levels: [protected]
  warn_levels: [watched]

zones:
  - path: "app/invalid/**"
    level: protected
    maturity: pilot
    reason: invalid maturity is enforcing
    required_approval: security-reviewer
