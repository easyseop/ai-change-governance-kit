# schemas/ — 증거 검증 스키마 (자리표시)

ADR-002 `acgh-result` 정본 결과계약이 구현되면 여기에
`acgh-result.schema.json`(shape) + semantic validator 규칙이 놓인다.
현재는 미구현 — `templates/change-evidence.template.yaml`(감사카드 스키마)이
증거 형식의 임시 정본. (kit README §알려진 갭 1·2 참조)
