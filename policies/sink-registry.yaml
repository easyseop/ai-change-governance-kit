# =====================================================================
# sink-registry.yaml · 명시 sink 등록 (MVP-2 간접영향 추적 대상)
# =====================================================================
# frozen 자동 sink 와 @gov(sink=true) 로 커버하지 못하는 함수만 등록한다.
# 신규 sink 는 shadow 로 관찰한 뒤 정책 소유자가 enforcing 으로 승격한다.
# =====================================================================

policy_version: "0.1-mvp2"

defaults:
  maturity: shadow
  hops: 1

sinks: []
